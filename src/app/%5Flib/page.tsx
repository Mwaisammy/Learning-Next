export default function PrivateFolder()
{
    return(
        <div>
            <h1>This folder cannot be viewed in the UI</h1>
        </div>
    )

}

//use _lib folder to make it not invisible in the UI  k