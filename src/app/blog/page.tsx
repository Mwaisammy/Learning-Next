import { Metadata } from "next"


export const metadata: Metadata = {
    title: 'Blog',
}

export default function Blog(){
    return(
        <div>
            <h1>Blogs here</h1>
        </div>
    )
}